import { SignalWeb } from '@/components/SignalWeb'

export const metadata = {
  title: 'Signal Web — DayTraders',
  description: 'Obsidian-style causal signal graph. See how subtle signals connect and tip the market balance — personalized to your trader profile.',
}

export default function SignalsPage() {
  return <SignalWeb />
}
