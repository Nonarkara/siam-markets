// Source of truth for the UI version stamp. Bump in package.json AND here
// on every meaningful release. Surfaced in TopNav (desktop) and below the
// DayTraders wordmark on mobile.

export const APP_VERSION = "1.2.0";
